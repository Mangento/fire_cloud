# Environment Configuration

# SMTP Configuration (Required for Postfix)
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@example.com

# Firebase Configuration
FIREBASE_PROJECT_ID=your-project-id

# For Firebase Functions, set these using:
# firebase functions:config:set smtp.host="localhost" smtp.port="25" smtp.secure="false" smtp.from="noreply@example.com"
# firebase functions:config:set smtp.user="your-user" smtp.pass="your-password"
